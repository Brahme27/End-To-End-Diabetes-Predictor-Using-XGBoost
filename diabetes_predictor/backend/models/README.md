# Backend models directory
# Trained ML models are stored here

This directory contains the trained machine learning model artifacts.

## Model Files

- `diabetes_model.joblib` - Trained Random Forest classifier
- Model will be generated by running the training pipeline

## Model Format

Models are serialized using `joblib` for efficient loading.

## Usage

The model is automatically loaded by the predictor service when the API starts.
